# For a local kind cluster only. On AWS, point ConfigMap's REDIS_URL at an
# ElastiCache endpoint instead and remove this file from your apply.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: pipelineops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: pipelineops
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
